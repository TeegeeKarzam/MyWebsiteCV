$('.container').fsScroll()


$('.container').fsScroll({
    loop:true,
    timing:'smooth',
duration:1000,
index:1,
selectors: {
    
        sections:'.sections',
    
        section:'.section',
    
        page:'.page',
    
        active:'.active'
    
      }}
    )
